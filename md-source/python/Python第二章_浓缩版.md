# Python 第二章 浓缩速记卡（变量 · 字符串 · 赋值）

---

## 一、变量与标识符

> **🧠 费曼：变量名 = 储物柜标签。** 你去健身房，前台给你一个柜子，贴个标签写上你的名字。标签可以换（重新赋值），柜子里放的东西也可以换（类型可变）。但你不能先把东西放进去再贴标签——必须先声明/赋值再用。Python比C语言懒：你不用提前说"我要一个放鞋的柜子"，直接扔东西进去，它自动识别类型。

### 1.1 变量核心规则

| 规则 | 说明 |
|------|------|
| **不需要声明类型** | 直接赋值即创建，`x = 5` 自动确定 x 为 int |
| **动态类型** | 同一变量可先后赋不同类型值：`x=5; x="hello"` ✅ |
| **先赋值再使用** | 未赋值的变量直接使用 → NameError |
| **值可变，类型可变** | 变量值在运行中可随意改 |

### 1.2 标识符命名规则（三必须 + 三不能）

**✅ 必须：**
- 首字符必须是 **字母 / 下划线 / 汉字**
- 后续字符可以是 **字母 / 数字 / 下划线 / 汉字**
- 区分大小写（`Name` ≠ `name` ≠ `NAME`）

**❌ 不能：**
- 不能以数字开头（`123abc` ❌）
- 不能含空格（`my var` ❌）
- 不能含特殊符号（`$var` / `my-var` / `a!1` ❌）
- 不能是关键字（`if` / `for` / `class` ❌）

### 1.3 合法 vs 非法 速查

| 合法 ✅ | 非法 ❌ | 原因 |
|---------|---------|------|
| `_myVar` | `123var` | 数字开头 |
| `MyGod5` | `5MyGod` | 数字开头 |
| `a_1` | `a 1` / `a!1` / `1a` | 空格/符号/数字开头 |
| `TempStr` | `if` | 关键字 |
| `_name` | `$var_1` | $ 非法字符 |
| `If` | `Draw Python` | 空格 |
| `Templist` | `33_keyword` | 数字开头 |
| `I` | `my-variable` | 连字符 - 不合法 |
| `α = 3.14` | — | Python3 支持 Unicode 标识符 ✅ |

### 1.4 关键字（保留字）

**概念：** Python 预定义的、有特殊含义的单词，**不能**当变量名。
**大小写敏感：** `if` 是关键字，但 `If` / `IF` 不是 → 可以当变量名。

| Python 关键字（部分常考） |
|---|
| `False` `True` `None` `and` `or` `not` |
| `if` `elif` `else` `for` `while` `break` `continue` |
| `def` `return` `class` `import` `from` `as` |
| `try` `except` `finally` `raise` `pass` `del` |
| `global` `nonlocal` `lambda` `with` `yield` `in` `is` |

**易混淆考点：**

| 名称 | 是不是关键字？ | 能当变量名？ |
|------|:--:|:--:|
| `if` | ✅ 是 | ❌ 不能 |
| `If` | ❌ 不是 | ✅ 能 |
| `elseif` | ❌ 不是（Python 用 `elif`） | ✅ 能 |
| `do` | ❌ 不是 | ✅ 能 |
| `type` | ❌ 不是（是内置函数） | ✅ 能（但不建议） |
| `string` | ❌ 不是 | ✅ 能 |
| `max` | ❌ 不是（是内置函数） | ✅ 能（但不建议） |
| `goto` | ❌ 不是 | ✅ 能 |

---

## 二、字符串与切片（重点！题量最大）

> **🧠 费曼：切片 = 切面包。** 你有一条长面包（字符串），想切出中间一段。你告诉师傅：从第3片开始切，切到第7片为止（但第7片不给你，只给到第6片！）。`s[3:7]` 就是这个意思——start包含，end不包含。如果你从右往左切（步长为负），那就是倒着切。`s[::-1]` = 把面包整个翻过来。

### 2.1 字符串基础

- 用 `" "` 或 `' '` 括起来
- `s="123"` 是字符串，`s=123` 是整数
- `"sm"` 是字符串常量，`sm` 是变量名 —— **引号决定一切**

### 2.2 索引（取单个字符）

```
正向索引： 0    1    2    3    4
          P    y    t    h    o    n
反向索引：-6   -5   -4   -3   -2   -1
```

| 表达式 | 结果 | 说明 |
|--------|------|------|
| `"Python"[0]` | `P` | 第一个 |
| `"Python"[4]` | `o` | 正向第5个 |
| `"Python"[-1]` | `n` | 最后一个 |
| `"Python"[-2]` | `o` | 倒数第二个 |

**中文字符串同理：**
- `"中华人民共和国"[3]` = `民`
- `"中华人民共和国"[-4]` = `民`
- 正向和反向都能取到同一字符

### 2.3 切片（取子串）—— 最核心

```
语法：s[start : end : step]
              ↑      ↑     ↑
           包含   不包含  步长(可省略，默认1)
```

**铁律：end 索引对应的字符不包含在结果中！**

| 表达式 | 结果 | 规律 |
|--------|------|------|
| `s[:]` | 完整字符串 | 全取 |
| `s[m:]` | 从 m 到末尾 | 左边界包含 |
| `s[:n]` | 从开头到 n-1 | 右边界**不包含** |
| `s[m:n]` | 从 m 到 n-1 | 取 n-m 个字符 |
| `s[-n:]` | 最后 n 个字符 | 常用技巧 |
| `s[:-n]` | 去掉最后 n 个 | 常用技巧 |
| `s[::k]` | 每隔 k-1 个取一个 | 跳步切片 |

### 2.4 切片高频考题

| 题目 | 答案 | 关键 |
|------|------|------|
| `"abcdef"[0:-1]` | `abcde` | -1 是 f 不包含 |
| `"Python"[ :3]` | `Pyt` | 0,1,2 → 不包含索引3 |
| `"abcde"[2:4]` | `cd` | 不是 `cde`！索引4(e)不包含 |
| `"abcde"[2:]` | `cde` | 从c到末尾 |
| `"Hello World"[6:]` | `World` | 空格算一个位置 |
| `"Python Programming"[-5:]` | `mming` | 最后5个字符 |

### 2.5 中文字符串切片

```
s = "河南财经政法大学"
索引:    0  1  2  3  4  5  6
字符:    河 南 财 经 政 法 大 学
反向:   -8 -7 -6 -5 -4 -3 -2 -1
```

| 表达式 | 结果 |
|--------|------|
| `s[0]` / `s[-8]` | `河` |
| `s[2:]` | `财经政法大学` |
| `s[-2:]` | `大学` |
| `s[:2]` | `河南` |
| `s[2:6]` | `财经政法` |
| `s[6:7]` → 只取 `大` ❌ | 想取"大学"用 `s[-2:]` |

### 2.6 切片填空题速算口诀

拿到字符串先数长度，正向标序号，反向从-1倒数。
**end不包含**是丢分第一原因。

---

## 三、赋值语句

### 3.1 基本赋值

```python
x = 5           # 基本赋值
x += 3          # 增量赋值，等价 x = x + 3
x *= 2          # 等价 x = x * 2
a %= b          # 等价 a = a % b
```

### 3.2 同步赋值（多变量同时赋值）

```python
x, y = 10, 20           # x=10, y=20
a, b, c = 1, 2, 3       # a=1, b=2, c=3
x, y = y, x             # 交换！x=20, y=10
a, b = b, a+b           # 右侧先全部算完再赋值
```

**关键规则：**
- 等号右侧所有表达式**先全部计算**，再一次性赋给左侧
- `a, b = b, a+b` ≠ `a=b; b=a+b` —— 同步赋值用原始值算，逐行赋值用更新值算

```python
# 同步赋值
x, y = 3, 5
x, y = y, x + y    # x=5, y=3+5=8

# 逐行赋值（不等价！）
x = y               # x=5
y = x + y           # y=5+5=10 → 结果不同！
```

### 3.3 链式赋值

```python
a = b = c = 10    # 三个变量都是10 ✅
```

### 3.4 赋值铁律

| 规则 | 示例 |
|------|------|
| 左侧必须是**变量** | `10 = a` ❌  `x+1 = x` ❌ |
| 右侧可以是表达式 | `x = y + 1` ✅ |
| 字符串字面量不能放左侧 | `"abc" = x` ❌ |

### 3.5 续行与分隔

- `\` 续行符：一行写不下时用
- `;` 分隔同行的多条语句（用英文分号，不能用中文）
- 注释行**不能**用 `\` 续行

---

## 四、特殊约定（判断题陷阱）

| 说法 | 对错 | 原因 |
|------|:--:|------|
| 全大写变量不可修改 | ❌ | 只是约定，Python不强制，改了不报错 |
| 下划线开头表示私有变量 | ✅ | PEP8 约定 |
| `True` / `False` 是关键字 | ✅ | Python3 新增保留字 |
| 变量类型一旦确定就不能改 | ❌ | Python是动态类型 |
| 标识符不能含汉字 | ❌ | Python3 支持 Unicode，汉字可以 |
| `goto` 是Python保留字 | ❌ | Python没有goto |
| 程序员可以随便给变量取名 | ❌ | 要遵守命名规则，不能用关键字 |

---

## 五、高频易错速查

| 陷阱 | 正确答案 |
|------|----------|
| `123abc` 能当变量名？ | ❌ 不能，数字开头 |
| `if` 能当变量名？ | ❌ 不能，是关键字 |
| `If` 能当变量名？ | ✅ 能，大小写不同 |
| `PI` 和 `Pi` 是同一个变量？ | ❌ 不同，区分大小写 |
| Python需先声明变量类型？ | ❌ 不需要 |
| 切片 `s[m:n]` 包含索引n？ | ❌ 不包含（end exclusive） |
| `a[m:m+5]` 取几个字符？ | 5个 |
| 想包含索引n，切片怎么写？ | `s[m:n+1]` |
| s[-1] 取的是什么？ | 最后一个字符 |
| 交换a和b的值怎么写？ | `a, b = b, a` |
| `a,b=b,a+b` 等价于 a=b;b=a+b？ | ❌ 不等价 |
| `x+1=x` 合法吗？ | ❌ 不合法 |
| 中文分号能分隔Python语句？ | ❌ 不能，只认英文 `;` |

---

## 六、自测15题（检测学习效果）

> 每题涵盖不同知识点，做完对答案。每题5分，70分及格。

**1.** 以下哪个变量名符合Python命名规则？

A. `123python`　　B. `my-var`　　C. `_temp`　　D. `$money`

**2.** 以下关于Python变量的说法，**错误**的是？

A. 变量不需要提前声明类型　　B. 变量类型可以动态改变  
C. 变量必须先赋值再使用　　D. 变量类型一旦确定就不能改

**3.** 以下哪个**不是**Python关键字？

A. `elif`　　B. `pass`　　C. `do`　　D. `try`

**4.** `If` 能作为Python变量名吗？

A. 能，因为Python区分大小写　　B. 不能，因为if是关键字  
C. 能，因为Python不区分大小写　　D. 不能，因为If是内置函数

**5.** 以下哪个赋值语句是**错误**的？

A. `x, y = 1, 2`　　B. `a = b = 10`　　C. `10 = x`　　D. `x += 5`

**6.** 执行 `x, y = 3, 5; x, y = y, x + y` 后，x和y的值分别是？

A. 3, 8　　B. 5, 8　　C. 5, 3　　D. 3, 5

**7.** 字符串 `s = "Hello World"`，`s[6:]` 的结果是？

A. `World`　　B. ` World`　　C. `W`　　D. `Hello`

**8.** 字符串 `s = "Python"`，`s[2:4]` 的结果是？

A. `th`　　B. `tho`　　C. `yt`　　D. `thon`

**9.** 设 `a = "abcde"`，`a[2:4]` 的结果是？

A. `cd`　　B. `cde`　　C. `bc`　　D. `bcd`

**10.** 字符串 `s = "中华人民共和国"`，取到 `民` 的正确方式是？

A. 只能 `s[3]`　　B. 只能 `s[-4]`　　C. `s[0]`　　D. `s[3]` 或 `s[-4]` 均可

**11.** 字符串 `s = "abcdef"`，`s[0:-1]` 的结果是？

A. `abcdef`　　B. `abcde`　　C. `bcdef`　　D. `f`

**12.** 串 `s = "河南财经政法大学"`，`s[-2:]` 的结果是？

A. `大学`　　B. `学`　　C. `大`　　D. `政法`

**13.** 执行 `a = 5; a += 3; a *= 2` 后，a的值是？

A. 10　　B. 13　　C. 16　　D. 8

**14.** 以下哪条语句能交换变量a和b的值？

A. `a = b; b = a`　　B. `a, b = b, a`　　C. `b = a; a = b`　　D. `a = b, b = a`

**15.** 字符串 `s = "PythonProgramming"`，`s[1:7]` 的结果是？

A. `Python`　　B. `ythonP`　　C. `ythonPr`　　D. `y`

---

### 答案与解析

| 题号 | 答案 | 解析 |
|------|------|------|
| 1 | **C** | `_temp` 下划线开头合法。A数字开头，B含连字符，D含$符。 |
| 2 | **D** | Python是动态类型语言，变量类型可以改变。`x=5; x="hello"` 完全合法。 |
| 3 | **C** | Python没有 `do` 关键字。`elif`/`pass`/`try` 都是保留字。 |
| 4 | **A** | Python区分大小写，`if` 是关键字，`If` 不是。可以当变量名（虽然不建议）。 |
| 5 | **C** | 赋值左侧必须是变量，`10` 是字面量，不能放等号左边。 |
| 6 | **B** | 同步赋值：右侧先全部计算（y=5, x+y=8），再赋给左侧（x=5, y=8）。 |
| 7 | **A** | H(0)e(1)l(2)l(3)o(4)空格(5)W(6)... `s[6:]` 从W取到末尾 = `World`。 |
| 8 | **A** | P(0)y(1)t(2)h(3)o(4)n(5)，`s[2:4]` 取索引2和3 = `th`。索引4(o)不包含！ |
| 9 | **A** | `a[2:4]` = `cd`。索引4(e)不包含，千万别选 `cde`。 |
| 10 | **D** | 中(0)华(1)人(2)民(3)共(4)和(5)国(6)。正向s[3]=民，反向s[-4]=民。两种都行。 |
| 11 | **B** | `s[0:-1]` 从头取到倒数第2个。索引-1是f不包含，结果=`abcde`。 |
| 12 | **A** | 河(0)南(1)财(2)经(3)政(4)法(5)大(6)学(7)。`s[-2:]` 取最后2个字符 = `大学`。 |
| 13 | **C** | a=5 → a+=3 → a=8 → a*=2 → a=16。增量赋值逐步执行。 |
| 14 | **B** | `a,b=b,a` 是Python交换两个变量的标准写法。A会丢失a的原值。 |
| 15 | **B** | P(0)y(1)t(2)h(3)o(4)n(5)P(6)... `s[1:7]` 从y(1)到P(6)不含P(7) = `ythonP`。 |

---

> 错3题以内：切片的 end不包含 掌握到位　｜　错3-6题：回头看切片2.3节　｜　错6题以上：全文重读，尤其字符串部分
