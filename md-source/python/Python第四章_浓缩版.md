# Python 第四章 浓缩速记卡（数字类型 · 运算符 · 内置函数）

---

## 一、数字类型三大类

> **🧠 费曼：数字三分天下——整数、小数、复数。** 整数=你数手指头（1,2,3...），怎么数都没小数点。浮点数=你量身高（1.75米），有小数点。复数=数学课本里的虚数，日常用不到但Python有它。记住口诀：`int`是整数，`float`带小数点，`complex`有个j。

| 类型 | 关键字 | 示例 |
|------|--------|------|
| 整数 | `int` | `123`, `0b101`, `0o77`, `0xFF` |
| 浮点数 | `float` | `3.14`, `1.23e-4`, `0.0` |
| 复数 | `complex` | `3+4j`, `complex(1,2)` |

---

## 二、整数的四种进制

| 进制 | 前缀 | 数码范围 | 示例 |
|------|------|----------|------|
| 十进制 | 无 | 0-9 | `123` |
| 二进制 | `0b` / `0B` | 0,1 | `0b1010` = 10 |
| 八进制 | `0o` / `0O` | 0-7 | `0o77` = 63 |
| 十六进制 | `0x` / `0X` | 0-9, a-f/A-F | `0xFF` = 255 |

### 关键规则

```python
0b1010      # ✅ 合法
0B1010      # ✅ 大写B也行
0b12        # ❌ 二进制只有0和1
0o89        # ❌ 八进制没有8和9
0x9g        # ❌ 十六进制没有g
08          # ❌ Python3不允许十进制以0开头
```

### 转换函数

```python
bin(100)    # '0b1100100'  十进制→二进制字符串
oct(100)    # '0o144'      十进制→八进制字符串
hex(100)    # '0x64'       十进制→十六进制字符串
```

### 十六进制转十进制速算

```
0x0101 = 1×256 + 0×16 + 1×1 = 257
0o1010 = 1×512 + 0×64 + 1×8 + 0×1 = 520
```

### 易错判断题

| 说法 | 对错 | 真相 |
|------|:--:|------|
| 二进制以0c开头 | ❌ | 以 **0b** 开头 |
| 八进制以0x开头 | ❌ | 八进制 **0o**，十六进制才是 **0x** |
| Python有十二进制 | ❌ | 只有十/二/八/十六进制 |
| 某些十进制不能转十六进制 | ❌ | **所有十进制整数都能转** |
| 0bC3F是合法二进制 | ❌ | 二进制只能0和1 |

---

## 三、浮点数

### 3.1 科学计数法

```python
1.23e4   = 12300.0       # e = ×10^
1.23e-4  = 0.000123      # e-4 = ×10⁻⁴
2.5e3    = 2500.0
123e+5   = 12300000.0    # ✅ 合法
123e     # ❌ 缺少指数
```

| 规则 | 说明 |
|------|------|
| `e` 或 `E` 都可以 | 大小写不敏感 |
| 指数必须是整数 | `1.2e3.5` ❌ |
| 不限定系数格式 | `123e4` / `1.23e6` / `0.123e7` 都合法 |

### 3.2 浮点数不确定尾数（头号考点）

> **🧠 费曼：0.1+0.2≠0.3，就像1/3在十进制里永远写不完。** 你用计算器算1÷3=0.33333...，写到纸上永远写不完。计算机用二进制存小数，0.1在二进制里也是个无限循环小数，存不下就截断，截断就有误差。所以0.1+0.2=0.30000000000000004。这跟Python没关系，是所有语言用IEEE 754的结果。

```python
0.1 + 0.2              # 0.30000000000000004 （不是0.3！）
0.1 + 0.2 == 0.3       # False！
```

**原因：** 某些十进制小数无法精确转换为二进制（如0.1在二进制中是无限循环小数），IEEE 754存储产生误差。

**应对：**
- `round(0.1+0.2, 2)` → `0.3`
- 使用 `decimal` 模块
- 不直接 `==` 比较浮点数

| 值的大小比较 | |
|------|------|
| `1.23e4` = 12300 | `12.3e3` = 12300 |
| `0.123e5` = 12300 | `123e3` = **123000** ← 最大 |

---

## 四、复数

```python
3 + 4j          # 复数，虚部用 j 或 J（不是 i！）
complex(10.99)  # (10.99+0j)
complex(45, 6)  # (45+6j)

# 属性
z = 3 + 4j
z.real          # 3.0 (float类型)
z.imag          # 4.0 (float类型)
```

| 说法 | 对错 |
|------|:--:|
| 虚部用 i 表示 | ❌ Python用 **j** 或 **J** |
| 虚部用 C/c 表示 | ❌ |
| 实部和虚部都是整数类型 | ❌ 都是 **float** 类型 |
| `3+4j` 合法 | ✅ |

---

## 五、算术运算符

### 5.1 七大运算符

| 运算符 | 含义 | 示例 | 结果 |
|--------|------|------|------|
| `+` | 加 | `3 + 2` | `5` |
| `-` | 减 | `3 - 2` | `1` |
| `*` | 乘 | `3 * 2` | `6` |
| `/` | 真除（返回float） | `5 / 2` | `2.5` |
| `//` | 整除（向下取整） | `5 // 2` | `2` |
| `%` | 取余（模） | `5 % 2` | `1` |
| `**` | 幂 | `2 ** 3` | `8` |

### 5.2 优先级（从高到低）

```
** → * / // % → + -
```

```python
3 + 2 * 3 ** 2    # **先算: 3**2=9 → 2*9=18 → 3+18=21
2 ** 3 * 4 % 5    # = 8*4%5 = 32%5 = 2
```

### 5.3 两个高频应用

```python
# 判断偶数
x % 2 == 0        # ✅ 正确
x // 2 == 0       # ❌ 错误（整除判偶不对）

# 取千位数字（四位数）
a // 1000         # ✅ 千位

# 取前两位+后两位（如9517 → 95+17）
a // 100 + a % 100                 # ✅ 95 + 17 = 112
b, c = divmod(a, 100); b + c       # ✅
int(a / 100) + a % 100             # ✅（a/100=95.17 → int→95）
a / 100 + a % 100                  # ❌（95.17 + 17 = 112.17）
```

### 5.4 常见应用题

```python
# 200张百元钞票均分给3人
200 // 3      # 66 每人几张
200 % 3       # 2  还剩几张

# 70万小时 = 多少周岁
70 * 10000 // 24 // 365

# 平均寿命78.9岁 = 多少小时
78.9 * 365 * 24
```

### 5.5 右结合性（**是特例）

```python
2 ** 3 ** 0    # 右结合：先算 3**0=1，再算 2**1=2
               # NOT (2**3)**0=1！
```

### 5.6 运算符判断题

| 说法 | 对错 |
|------|:--:|
| `//` 是整除，`/` 是浮点除 | ✅ |
| `**` 的 y 必须是整数 | ❌ （`4**0.5`=2.0 合法） |
| `a**b` 只能用 `**` 算 | ❌ 也可用 `pow(a,b)` |
| 整除只能用 `a//b` 和 `a%b` | ❌ 也可用 `divmod(a,b)` |
| `&` 是算术运算符 | ❌ 是位运算符 |
| `9 / 3` 结果是 int | ❌ Python3 中 `/` 总是返回 **float**（3.0） |
| 整数与浮点数运算结果是整数 | ❌ 结果是 **float** |

---

## 六、内置数值函数速查

### 6.1 类型转换

| 函数 | 作用 | 示例 |
|------|------|------|
| `int(x)` | 转整数（截断） | `int(3.9)` → `3`, `int("123")` → `123` |
| `float(x)` | 转浮点数 | `float(5)` → `5.0`, `float("3.14")` → `3.14` |
| `complex(a,b)` | 创建复数 | `complex(3,4)` → `(3+4j)` |

```python
int("123")       # 123 ✅
int("123.45")    # ❌ ValueError（不能直接转带小数的字符串）
float("123.45")  # 123.45 ✅
int(True) = 1, int(False) = 0
float(True) = 1.0
```

### 6.2 数学运算

| 函数 | 作用 | 示例 |
|------|------|------|
| `abs(x)` | 绝对值 | `abs(-3.14)` → `3.14` |
| `pow(x, y)` | x的y次方 | `pow(2,3)` → `8` |
| `pow(x, y, z)` | x**y % z（高效） | `pow(3,3,2)` → `1` |
| `round(x, n)` | 四舍五入保留n位 | `round(3.14159,2)` → `3.14` |
| `divmod(a,b)` | 返回(商, 余数) | `divmod(20,3)` → `(6,2)` |
| `max(...)` | 最大值 | `max(10,20,5)` → `20` |
| `min(...)` | 最小值 | `min(5,3,-10)` → `-10` |
| `sum(iterable)` | 求和 | `sum([1,2,3])` → `6` |

### 6.3 字符编码

| 函数 | 作用 | 示例 |
|------|------|------|
| `ord(c)` | 字符→Unicode编码 | `ord('A')`→`65`, `ord('a')`→`97`, `ord('z')`→`122` |
| `chr(n)` | 编码→字符 | `chr(65)`→`'A'`, `chr(97)`→`'a'` |

```python
ord('A') + 2 = 67    chr(67) = 'C'
ord('z') = 122       chr(65+32) = chr(97) = 'a'
```

### 6.4 函数陷阱

| 陷阱 | 真相 |
|------|------|
| `round()` 能解决所有浮点误差？ | ❌ 不能全解决，decimal模块才行 |
| `max()`只能接受两个参数？ | ❌ 可以多个参数或可迭代对象 |
| `divmod(10,3)` 返回 `(3,1)` | ✅ 商3余1 |
| Python3默认编码是ASCII？ | ❌ 是 **Unicode(UTF-8)** |
| `int(2.6)` = 3？ | ❌ = **2**（截断，不四舍五入） |
| `pow(3,2) == 3**2`？ | ✅ True |
| `9999**9999` 会报错？ | ❌ Python支持大整数 |

---

## 七、综合填空题速算表

| 表达式 | 结果 |
|--------|------|
| `19%5` | `4` |
| `19//5` | `3` |
| `3/3` | `1.0` |
| `2**3**0` | `2` |
| `18%5 + 18//5*5` | `18` |
| `x=3; x*=2+3**2` | `33`（先算3²+2=11，再×3） |
| `(10**2 + 3*5)//(4-1)` | `38` |
| `(5*2**3) - (16/4)` | `36.0` |
| `sum(range(5))` | `10` |
| `pow(2,-1)` | `0.5` |
| `max("hello")` | `'o'` |
| `min("Python")` | `'P'`（大写字母ASCII值小于小写） |

---

## 八、高频易错速查

| 陷阱 | 正确答案 |
|------|----------|
| 0.1+0.2 == 0.3？ | **False** |
| 0.1+0.2 的结果？ | **0.30000000000000004** |
| `9/3` 的类型？ | **float**（Python3中/总返回float） |
| 二进制前缀？ | **0b** 不是 0c |
| 八进制前缀？ | **0o** 不是 0x |
| `0x0101` 的值？ | **257** |
| `0o1010` 的值？ | **520** |
| 复数虚部用什么字母？ | **j** 或 **J**，不是 i |
| 判断偶数怎么写？ | `x%2==0` 不是 `x//2==0` |
| `**` 的结合性？ | **右结合** |
| Python3默认字符编码？ | **Unicode** 不是 ASCII |
| `int(2.6)` 结果？ | **2**（截断不是四舍五入） |
| `int("123.45")` 结果？ | **报错** ValueError |
| `eval('500/10')` 结果？ | **50.0**（float） |
| `123e` 合法？ | **❌** 缺少指数 |

---

## 九、自测15题（检测学习效果）

> 每题5分，70分及格。重点覆盖浮点误差、进制、运算符优先级。

**1.** `0.1 + 0.2 == 0.3` 的结果是？

A. True　　B. False　　C. 报错　　D. 0.3

**2.** Python中合法的二进制整数是？

A. `0b12`　　B. `0b1010`　　C. `0x1010`　　D. `0o1010`

**3.** `int(3.9)` 的结果是？

A. 4　　B. 3.9　　C. 3　　D. 4.0

**4.** `9 / 3` 的结果类型是？

A. int　　B. float　　C. str　　D. bool

**5.** 以下哪个**不是** Python 整数表示方式？

A. 十进制　　B. 二进制　　C. 十二进制　　D. 十六进制

**6.** `0x0101` 的十进制值是？

A. 101　　B. 257　　C. 65　　D. 17

**7.** `3 + 2 * 3 ** 2` 的结果是？

A. 45　　B. 21　　C. 15　　D. 75

**8.** 判断 x 是偶数，正确的是？

A. `x // 2 == 0`　　B. `x % 2 == 0`　　C. `x / 2 == 0`　　D. `x % 2 == 1`

**9.** `ord('A')` 的值是？

A. 97　　B. 66　　C. 65　　D. 98

**10.** `pow(3, 3, 2)` 的结果是？

A. 27　　B. 9　　C. 1　　D. 0

**11.** Python中复数的虚部用什么字母？

A. i　　B. j　　C. c　　D. v

**12.** `round(3.14159, 2)` 的结果是？

A. 3.14　　B. 3.15　　C. 3.1　　D. 3

**13.** `divmod(20, 3)` 的结果是？

A. `(6, 2)`　　B. `6.66`　　C. `6`　　D. `2`

**14.** `bin(100)` 的返回值是？

A. `'1100100'`　　B. `'0b1100100'`　　C. `'0o144'`　　D. `'0x64'`

**15.** `a=10; x=3; a*=2+3**2` 后 a 的值是？

A. 50　　B. 23　　C. 110　　D. 33

---

### 答案与解析

| 题号 | 答案 | 解析 |
|------|------|------|
| 1 | **B** | IEEE 754 浮点误差→0.1+0.2=0.30000000000000004，不等于0.3。判断浮点数相等是经典坑。 |
| 2 | **B** | `0b1010`=10。A含非法数字2，C是十六进制，D是八进制。 |
| 3 | **C** | int() 向零截断，不四舍五入。3.9→3，-3.9→-3。 |
| 4 | **B** | Python3 中 `/` 永远返回 float，即使整除也是 3.0。`//` 才返回 int。 |
| 5 | **C** | Python 整数四大进制：十(无前缀)、二(`0b`)、八(`0o`)、十六(`0x`)。没有十二进制。 |
| 6 | **B** | `0x0101` = 1×16² + 0×16¹ + 1×16⁰ = 256+0+1 = 257。 |
| 7 | **B** | 优先级：`**` > `*` > `+`。3²=9, 2×9=18, 3+18=21。 |
| 8 | **B** | 偶数÷2余0→`x%2==0`。A是错的（x//2==0 只在 x=0或1时成立）。 |
| 9 | **C** | A的Unicode码=65。a=97，z=122。务必记住A=65、a=97。 |
| 10 | **C** | `pow(3,3,2)` = 3³%2 = 27%2 = 1。第三个参数是取模。 |
| 11 | **B** | Python 用 `j` 或 `J`。数学课本用 i，Python 用 j。别搞混。 |
| 12 | **A** | round(3.14159, 2) = 3.14（保留两位小数，四舍五入）。 |
| 13 | **A** | divmod(被除数, 除数) 返回元组 `(商, 余数)`。20÷3=6余2→`(6,2)`。 |
| 14 | **B** | bin() 返回带 `0b` 前缀的字符串。不带前缀的是数字本身。 |
| 15 | **C** | `a*=2+3**2` → 先算右边：3²=9，2+9=11，然后 a=10×11=110。增强赋值的右操作数先整算。 |

---

> 错3题以内：进制和运算符过关了　｜　错3-6题：重点回看第二节(进制)和第五节(运算符)　｜　错6题以上：全文重读
